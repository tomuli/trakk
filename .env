# Shared environment variables can go here.
# Environment-specific values live in .env.development / .env.test
# and are loaded automatically via the Makefile helpers.
#
# Better Auth requires a long random secret (>=32 chars). Copy the example
# value from .env.development/.env.test or set BETTER_AUTH_SECRET manually.
# BETTER_AUTH_URL should point to the public URL (or localhost) hosting the app.
